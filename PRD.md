# NUEAT — Product Requirements Document

> **Positioning:** Know what to eat next.  
> **Status:** Implementation-ready draft  
> **Version:** 1.0 · **Market:** Korea-first MVP  
> **Primary readers:** Solo founder · Frontend/code agents  
> **Updated:** 2026-08-10

> **제품 한 문장:** 사진으로 먹은 것을 기록하고, 신뢰 가능한 영양 데이터로 오늘의 부족분을 계산해 다음에 무엇을 먹을지 알려주는 AI 영양 코치.

## 1. Executive summary

NUEAT은 한국 식문화에 맞춘 개인 영양 의사결정 앱이다. 사용자는 목표를 설정하고 식사 사진을 찍은 뒤 음식과 섭취량을 빠르게 교정한다. 시스템은 출처가 추적되는 데이터베이스 값으로 열량·탄수화물·단백질·지방·식이섬유를 계산하고, 일일 목표 대비 격차를 보여주며, “다음에 무엇을 먹을까?”에 실행 가능한 식사 조합으로 답한다.

> **핵심 제품 원칙:** AI는 음식을 인식하고 설명하지만 영양 수치를 만들어내지 않는다. 모든 표시 값은 음식 레코드·섭취량·영양 DB 버전까지 역추적 가능해야 한다.

| 구분 | MVP 성공 정의 |
| --- | --- |
| 행동 | 신규 사용자가 첫 세션에 목표 설정 → 사진 기록 → 양 수정 → 대시보드 확인을 완료한다. |
| 가치 | 사용자가 기록 후 60초 안에 오늘의 핵심 부족분과 다음 식사 선택지를 이해한다. |
| 신뢰 | 영양 값의 출처/추정 상태가 명확하고, 사용자가 음식·양을 언제든 수정할 수 있다. |
| 사업 | 반복 기록과 추천 조회가 주간 습관으로 형성되고 유료 의향을 검증한다. |

## 2. 문제, 비전, 가치 제안

### 2.1 문제 정의

- 칼로리 기록은 입력 비용이 높고, 한국 혼합식·반찬·외식 메뉴는 검색과 양 추정이 어렵다.
- 기존 앱은 “오늘 몇 kcal 먹었는가”는 보여주지만 “그래서 다음에 무엇을 먹어야 하는가”까지 연결하지 못하는 경우가 많다.
- 생성형 AI가 근거 없이 영양 수치를 말하면 정확도와 신뢰가 무너진다.
- 보충제·질환 관련 조언이 생활 습관 코칭과 섞이면 안전 및 규제 리스크가 커진다.

### 2.2 비전

누구나 식사 직후 30초 안에 기록을 끝내고, 하루의 남은 식사를 자기 몸의 목표에 맞게 선택할 수 있게 한다. 장기적으로는 단순 식단 기록기가 아니라 개인의 식사 이력·선호·생활 맥락을 이해하는 일상 영양 의사결정 계층이 된다.

### 2.3 가치 제안

| 사용자 가치 | NUEAT의 답 |
| --- | --- |
| 빠른 기록 | 사진 인식 → 후보 음식 → 양 교정의 3단계 흐름 |
| 신뢰 가능한 숫자 | 검증된 DB 조회 및 계산, 출처·버전·추정치 표시 |
| 행동 가능한 피드백 | 부족 영양소를 음식/식사 조합과 목표 달성 변화로 번역 |
| 한국 적합성 | 밥·국·찌개·반찬·혼합식과 1인분 관행을 우선 지원 |

## 3. 목표 사용자와 JTBD

| 세그먼트 | 상황/욕구 | 핵심 JTBD |
| --- | --- | --- |
| 핵심: 건강관리 입문자 | 다이어트·근육 증가·균형 식사를 원하지만 매크로 계산은 부담 | 식사 사진만으로 오늘의 상태를 알고 다음 선택을 개선하고 싶다. |
| 성과형 사용자 | 운동을 하며 단백질·열량 목표를 관리 | 정확한 양 수정과 일일 잔여 목표를 빠르게 확인하고 싶다. |
| 식단 품질 관심자 | 체중보다 섬유질·균형·식품 다양성을 중시 | 칼로리 외 영양 격차를 이해하고 현실적인 메뉴를 추천받고 싶다. |
| 비목표 사용자 | 임신·수유, 미성년자, 섭식장애 위험, 질환 치료 목적 | MVP 자동 코칭 대상에서 제외하거나 전문가 상담을 안내한다. |

### 3.1 대표 JTBD

- 식사를 마쳤을 때, 번거로운 검색 없이 먹은 것을 기록해 내 목표 진행률을 알고 싶다.
- 하루 중간에, 부족한 영양을 확인해 다음 식사의 재료와 양을 결정하고 싶다.
- AI 인식이 틀렸을 때, 몇 번의 탭으로 음식과 양을 고쳐 정확도를 통제하고 싶다.
- 추천을 받을 때, 왜 추천됐는지와 목표가 얼마나 개선되는지 알고 싶다.

## 4. 경쟁 맥락과 포지셔닝

경쟁 제품 분석은 공개 포지셔닝을 기준으로 한 방향성 비교이며, 출시 전 최신 기능·가격·한국 지원 범위를 재검증한다.

| 제품 | 강점/인식 | NUEAT이 차별화할 지점 |
| --- | --- | --- |
| Cal AI | 사진 중심의 낮은 기록 마찰 | 한국 음식·근거 DB·영양 격차→다음 식사 연결 |
| SnapCalorie | 이미지 기반 섭취량/영양 추정 | 사용자 교정과 출처 추적을 제품 핵심으로 |
| Cronometer | 상세 영양소와 데이터 신뢰성 | 입력 부담을 사진 UX로 줄이고 한국 식품 강화 |
| Nutrola | AI 기반 영양 기록/코칭 범주 | 추천 근거와 목표 변화량을 명시 |
| Foodvisor | 사진 기록·코칭·콘텐츠 결합 | 한국 우선 식문화와 간결한 행동 루프 |
| Kurly Roution | 한국 생활/식품 접점 가능성 | 독립적인 개인 영양 격차 엔진과 교정 가능한 기록 |

> **포지셔닝:** “가장 정확해 보이는 사진 칼로리 앱”보다 “신뢰할 수 있는 데이터로 다음 식사를 결정해 주는 Korea-first 영양 코치”를 지향한다.

## 5. 제품 범위

### 5.1 MVP 포함

| 영역 | MVP 산출물 | P |
| --- | --- | --- |
| 계정/동의 | Better Auth 기반 이메일 OTP 로그인, 약관·개인정보·건강정보 동의 | P0 |
| 온보딩 | 목표, 신체정보, 활동, 식이 선호/알레르기, 기본 영양 목표 | P0 |
| 사진 기록 | 카메라/앨범, 복수 음식 후보, 확인·삭제·추가 | P0 |
| 양 교정 | g/ml/인분/공기/조각 등 단위, 빠른 비율 조정 | P0 |
| 영양 계산 | 열량·탄/단/지·식이섬유, 출처 및 추정 표시 | P0 |
| 일일 대시보드 | 섭취/목표/잔여, 핵심 격차, 식사 타임라인 | P0 |
| 다음 식사 추천 | 2–3개 조합, 이유, 목표 변화, 교체/제외 | P0 |
| 피드백 | 인식·추천 유용성, 오류 신고 | P1 |
| 미량영양소 | DB 커버리지가 확보된 항목부터 베타 노출 | P2 |

### 5.2 MVP 제외

- 질환 진단·치료, 약물 조정, 임상 영양 처방, 검사 결과 해석
- 보충제 용량 추천·구매 최적화, 상호작용 판단
- 연속혈당측정기·웨어러블 자동 연동, 가족/코치 계정, 커뮤니티
- 식당 메뉴 전체 데이터베이스, 영수증 인식, 배달 주문 연동
- 완전 자동 기록: 사용자의 최종 확인 없이 영양 섭취를 확정하지 않음

## 6. 상세 사용자 여정

### 6.1 첫 사용과 온보딩

1. 가치 제안: “오늘의 영양 격차를 보고, 다음 식사를 결정하세요.”
2. 안전/개인정보 요약 및 필수 동의. 건강정보 처리 목적·보관·삭제 정책을 분리 표시.
3. 주 목표 선택: 체중 감량, 유지, 근육 증가, 균형 식사. 목표 속도는 안전 범위 내에서 제한.
4. 필수 정보: 출생연도/연령대, 성별(계산상 필요한 경우 설명), 키, 체중, 활동 수준. 선택 정보는 건너뛰기 가능.
5. 식이 선호, 알레르기, 제외 음식, 자주 먹는 식사 환경을 수집.
6. 계산된 열량·영양 목표를 설명과 함께 제시하고 사용자가 확인/조정. “의료 처방이 아님”을 명시.
7. 샘플 대시보드 후 카메라 권한을 맥락적으로 요청하고 첫 식사 기록으로 연결.

> **온보딩 완료 기준:** 유효한 목표 프로필 저장 + 필수 동의 완료 + 홈 화면 진입. 목표 생성이 불가능한 위험 조건은 일반 건강 모드 또는 전문가 안내로 전환.

### 6.2 음식 사진 기록

1. 사용자가 홈의 “식사 기록”을 누르고 식사 시점/종류를 선택한다.
2. 사진 촬영 또는 앨범 선택. 업로드 중 개인정보·배경 노출 안내와 취소 기능 제공.
3. 비전 모델이 음식 영역과 후보명을 반환한다. 낮은 신뢰도 항목은 질문 또는 검색으로 전환한다.
4. 확인 화면에서 음식별 사진 영역, 이름, 기본 양, 단위를 보여준다. 사용자는 추가·교체·삭제·양 조정한다.
5. 확정 시 음식 레코드와 DB 영양 프로필을 결합해 섭취량을 계산하고 출처/추정 상태를 저장한다.
6. 결과 화면에서 총열량·매크로·식이섬유와 오늘 목표 변화, 기록 수정 진입점을 보여준다.

### 6.3 영양 대시보드

- 상단: 날짜, 목표 모드, 오늘 기록 진행 상태.
- 핵심 카드: kcal, 단백질, 식이섬유의 섭취/목표/잔여. 탄수화물·지방은 접거나 확장.
- 격차 우선순위: 절대 부족량만이 아니라 시간대, 남은 열량, 선호/제약을 고려해 1–3개 표시.
- 식사 타임라인: 사진 썸네일, 식사 합계, 편집/삭제. 삭제는 확인 및 되돌리기 제공.
- 데이터 품질: 확정·추정·미확인 배지와 영양 데이터 출처 보기.

### 6.4 “다음에 무엇을 먹을까?”

1. 사용자가 CTA를 누르면 현재 시각, 남은 목표, 최근 식사, 선호/알레르기, 제외 음식으로 추천 요청을 생성한다.
2. 규칙/최적화 계층이 목표를 크게 초과하지 않는 후보 식사 조합과 권장 양을 만든다.
3. 생성형 AI는 후보를 자연어로 설명하되 음식·영양 수치는 계산 엔진 결과만 사용한다.
4. 카드 2–3개에 조합, 양, 예상 영양, “단백질 32g 보충 / 섬유질 목표 68→91%” 같은 이유를 표시한다.
5. 사용자는 싫어요/교체/집에 있는 재료/외식 모드를 선택한다. “먹었어요”는 새 기록 초안으로 만들고 재확인한다.

## 7. 기능 요구사항 및 수용 기준

| ID | 요구사항 / 수용 기준 | P |
| --- | --- | --- |
| FR-01 | 목표 프로필을 생성·수정하고 변경 시 이후 목표만 재계산한다. 과거 섭취 원본은 보존. | P0 |
| FR-02 | 사진 업로드 실패·권한 거부·오프라인 시 재시도 또는 수동 검색 경로 제공. | P0 |
| FR-03 | 인식 결과는 복수 항목·후보·confidence를 반환하며 최종 저장 전 확인 필수. | P0 |
| FR-04 | 음식명, 양, 단위를 수정하면 영양 합계와 대시보드가 즉시 재계산된다. | P0 |
| FR-05 | 영양 값마다 source, source_item_id, dataset_version, basis_per_100g, 계산 시각 저장. | P0 |
| FR-06 | 일별 합계는 동일 타임존의 확정 기록만 집계하고 목표 대비 잔여를 표시. | P0 |
| FR-07 | 추천은 알레르기·제외 식품을 hard constraint로 적용하고 이유/예상 효과를 표시. | P0 |
| FR-08 | 사용자는 기록과 계정을 삭제할 수 있고, 삭제 상태 및 보존 예외를 안내. | P0 |
| FR-09 | 인식 오류·DB 불일치·추천 피드백을 원본 컨텍스트와 함께 수집. | P1 |
| FR-10 | 운영자는 음식 매핑·DB 우선순위·추천 금칙 규칙을 버전 관리. | P1 |
| FR-11 | 신뢰 기준 미달 값은 “확인 필요”로 표시하고 숫자를 단정하지 않는다. | P0 |

## 8. 데이터 모델

| 엔터티 | 핵심 필드 |
| --- | --- |
| User/UserProfile | Better Auth identity, locale, timezone, deletion_status, onboarding_status(pending/completed/limited), safety_mode_reason_codes, onboarding_completed_at |
| Consent | user_id, type, action, document_version, document_sha256, occurred_at; append-only |
| NutritionProfile | user_id, goal/body inputs, integer nutrient targets, KDRI·정오표·engine·safety versions, input/macro snapshots, EER/활동계수, effective_from/to |
| DietaryConstraint | type(allergy/preference/exclusion), food_id/label, severity, source |
| ImageAsset | user_id, private bucket/object_key, validation status, declared/detected type, size, dimensions, sha256, expiry; URL 저장 금지 |
| MealLog | user_id, eaten_at/timezone/local_date, meal_type, status, image_asset_id, deletion/purge timestamps |
| MealItem | meal_log_id, recognized_label, food/nutrient_profile ids, integer amount/grams, component confidences, user_corrected |
| CalculationSnapshot | meal_log_id, immutable input_snapshot, integer totals, calculation_version, calculated_at |
| Food/FoodAlias | canonical Korean name, normalized aliases, category, preparation, composite/deprecation state |
| FoodServing | food_id, unit, integer amount/grams, source_registry_id, quality_grade |
| SourceRegistry | source code/kind, dataset_version, license reference, publication/import timestamps |
| NutrientProfile | food/source ids, source_item_id, dataset_version, integer basis/nutrients, nullable missing values, quality_grade |
| Recommendation | context_snapshot, candidate_items, projected totals, engine/model/prompt versions, safety_flags |
| Feedback | target_type/id, rating, reason_code, correction, free_text |
| AnalyticsEvent | user/anonymous session ids, allowlisted properties, occurred_at; 민감정보·OTP·signed URL 제외 |

계산 원칙: 표시 단위의 1은 `amount_milliunits=1,000`, 질량은 mg 정수로 저장한다. g 입력은 `amount_milliunits=grams_mg`이며 ml·인분·공기·조각은 출처가 있는 정확한 `FoodServing` 변환 없이는 계산하지 않는다. 영양값은 `grams_mg × profile_value ÷ basis_mg`를 BigInt 중간값과 양수 half-up 반올림으로 계산해 부동소수점·overflow를 방지한다. 결측값은 `null`이며 0으로 간주하지 않는다. 한 항목이라도 특정 영양소가 결측이면 해당 식사 총계는 `null/partial`로 표시하되 알려진 합계와 결측 항목 수를 함께 유지한다. 확정 시 원본 영양 프로필·출처·데이터셋·계산 버전과 입력값을 immutable snapshot으로 보존한다. 목표 변경은 기존 `NutritionProfile`을 수정하지 않고 유효기간을 종료한 뒤 새 버전을 생성한다.

## 9. AI 및 영양 데이터 아키텍처

### 9.1 파이프라인

이미지 전처리 → 음식 영역/후보 인식 → 한국어 별칭 정규화 → canonical Food 매핑 → 양 후보 산정 → 사용자 교정 → DB 조회 → 결정론적 영양 계산 → 일일 격차 엔진 → 제약 기반 후보 생성 → 자연어 설명.

### 9.2 AI 원칙

- LLM/비전 모델 출력과 영양 사실 계층을 분리한다. 생성 모델은 DB에 없는 수치를 채우지 않는다.
- 모델 confidence보다 제품 confidence를 사용한다: 이미지 품질, 후보 간격, DB 매핑 품질, 양 추정 불확실성을 합성한다.
- 낮은 확신은 질문·검색·범위 표시로 처리한다. 예: “김치찌개로 보이나 확인이 필요해요.”
- 추천은 먼저 규칙과 수치 최적화로 안전한 후보를 만들고, AI는 설명·대안 표현을 담당한다.
- 모델·프롬프트·DB·규칙 버전을 기록하고, 골든셋 회귀 테스트 없이 교체하지 않는다.

### 9.3 한국 음식/DB 전략

- 1차 근거: 식품의약품안전처(MFDS) 등 공공 식품영양성분 데이터에서 식품명, 100g 기준 영양, 출처·공개 버전을 수집·정규화한다.
- 보완: 제조사 라벨/공식 공개 정보, 검증된 상용 DB를 출처 우선순위와 라이선스에 따라 병합한다. 사용자 입력은 별도 등급.
- 한국 음식 ontology: 음식명/별칭/띄어쓰기, 조리법, 국물 섭취 여부, 주재료, 브랜드·메뉴, 대표 제공량을 분리한다.
- 혼합식은 검증된 완성식 프로필을 우선 사용하고, 없으면 레시피 구성으로 계산하되 “레시피 추정”으로 표시한다.
- 초기 커버리지 우선순위: 밥류 → 국/찌개 → 면 → 구이/볶음 → 반찬 → 카페/편의점 → 체인 메뉴.
- 미량영양소는 항목별 결측률과 품질을 평가해 충분한 항목만 표시한다. 결측값을 0으로 취급하지 않는다.

> **출시 전 확인:** MFDS 데이터셋의 최신 이용조건, 갱신 주기, 필드 정의를 법적·기술적으로 검토하고 source registry에 기록한다.

### 9.4 확정 인프라

- 클라이언트는 Expo/React Native, API는 Railway의 Bun/Fastify 서비스로 운영한다.
- 인증은 Better Auth의 이메일 OTP만 사용하며 소셜 로그인과 비밀번호 로그인은 제공하지 않는다. 영속 데이터는 공유 Neon PostgreSQL 18을 사용하고 개발·migration도 로컬 DB 없이 해당 Neon 인스턴스에 직접 연결한다.
- OTP 메일은 Resend에서 `NUEAT <auth@boseong.dev>` 발신자로 전송하고, 운영 API의 기본 URL은 `https://api-nueat.boseong.dev`로 사용한다.
- 모노레포의 패키지 매니저·스크립트·테스트와 API 런타임은 Bun으로 통일한다. Expo 도구 호환을 위해 개발·CI 환경에는 Node.js LTS도 유지한다.
- API는 `/health/live`, Neon을 확인하는 `/health/ready`, Better Auth `/api/auth/*`, 인증 사용자 `/api/me`를 제공한다. 오류 응답은 request ID를 포함한 안정적인 공통 계약을 사용한다.
- OTP는 6자리·5분 만료·3회 시도·재발급 시 회전·해시 저장을 적용하며 발송 요청은 60초당 3회로 제한한다. 전역 rate limit은 Neon DB에 저장해 Railway 다중 인스턴스에서도 공유한다.
- Expo 앱은 `@better-auth/expo`와 SecureStore로 세션 쿠키를 보관하고 앱 시작 시 복원한다. 미인증 사용자는 제품 탭 대신 이메일→OTP 화면을 보고, OTP 재발송은 60초 대기·로컬 3회 실패 UX를 적용한다.
- 로그아웃 시 서버 세션을 종료하고 SecureStore 인증 쿠키를 제거한다. AsyncStorage·분석 이벤트·React 상태에 세션 토큰을 영구 저장하지 않는다.
- Railway는 root Dockerfile로 API만 빌드하고 pre-deploy migration 후 `/health/ready`를 통과해야 배포를 완료한다.
- 식사 이미지는 비공개 Railway Bucket에 저장한다. DB에는 영구 URL이 아닌 object key와 검증된 메타데이터만 저장한다.
- 업로드 전 API가 사용자 인증과 권한을 확인하고 서버가 object key를 생성한 뒤 짧은 만료 시간의 presigned upload 정보를 발급한다. 콘텐츠 형식과 최대 크기를 서명 조건으로 제한한다.
- 조회 시 소유권을 다시 확인한 뒤 짧은 만료 시간의 presigned GET URL을 발급한다. signed URL은 저장하거나 분석 이벤트에 기록하지 않는다.
- 업로드 완료 후 MIME 선언만 신뢰하지 않고 실제 파일 시그니처·크기·디코딩 가능 여부를 검증한 뒤 인식 파이프라인에 전달한다.
- 식사 또는 계정 삭제 시 연결된 이미지도 삭제하며, 실패한 삭제 작업은 재시도·감사 가능하게 기록한다.
- AI 제공자와 모델은 아직 확정하지 않는다. 한국 음식 골든셋의 정확도·지연·비용·구조화 출력 안정성을 비교한 뒤 결정한다.

### 9.5 이미지 수명주기

- 앱은 업로드 전에 이미지를 decode/re-encode하여 EXIF/GPS를 제거하고, 긴 변 1,600px 이하·10MB 이하로 축소한다. 업로드 성공 후 로컬 임시 파일을 삭제하며 오프라인 초안은 최대 24시간 보관한다.
- API는 인증 후 서버가 생성한 불투명 object key에 대해 5분 만료 presigned upload를 발급한다. 업로드 완료 시 실제 파일 시그니처·크기·해상도·디코딩 가능 여부와 SHA-256을 검증한다.
- 추론용 이미지는 비공개 상태로 처리하고 식사 확정 또는 처리 종료 후 최대 24시간 이내 삭제한다. 외부 AI에는 바이너리 전달을 우선하며 signed URL이 필요하면 1–2분으로 제한하고 로그에 남기지 않는다.
- 식사 타임라인에는 EXIF가 제거된 최대 512px 음식 중심 썸네일만 사용한다. 썸네일은 식사 기록 삭제 시 함께 삭제한다.
- 삭제 요청 즉시 새 signed GET 발급과 일일 집계를 차단한다. object 삭제는 transactional deletion job으로 재시도하며 24시간 이내 완료를 목표로 한다.
- 미사용 upload는 1시간, 미확정 draft는 24시간 후 정리한다. 계정 삭제는 세션 폐기와 접근 차단 후 모든 원본·썸네일 삭제가 완료되어야 사용자 행을 제거할 수 있다.
- 이미지·base64·signed URL·object key·EXIF·이메일은 분석 및 오류 로그에 기록하지 않는다. 이미지 학습 사용은 별도 opt-in 전까지 금지한다.
- 업로드 API는 인증된 `POST /api/image-assets/upload-intents` → signed PUT → `POST /api/image-assets/:id/complete` 순서다. 완료 API가 실제 객체를 다시 읽어 계약 일치, 디코딩, 형식, 1,600px 제한, 민감 EXIF/GPS 제거, SHA-256을 검증한 뒤에만 `validated`로 전환한다.
- 모바일은 원본을 그대로 업로드하지 않는다. 재인코딩 과정에서 원본 위치·기기·촬영시각 등 식별 메타데이터를 제거하고, 서버는 GPS와 허용된 정규화 방향/크기 필드 외 EXIF를 거부한다. 네트워크 실패·취소 시 로컬 초안 하나를 최대 24시간 보존한다.

### 9.6 영양 목표 계산 기준

- 목표 엔진은 보건복지부·한국영양학회의 `2025 한국인 영양소 섭취기준`과 2026-03-16 정오표를 적용한다. 구현 버전은 `nutrition-targets-v1`, 안전 규칙은 `nutrition-safety-v1`이다.
- 성인 EER은 KDRI의 나이·계산상 성별·신장·체중·활동계수 산출식을 사용한다. 계산상 성별은 공식 식 선택에만 사용하며 미제공 시 자동 목표를 만들지 않는다.
- 균형/유지는 EER, 감량은 15% 감량·최대 500kcal 적자·여성 1,200/남성 1,500kcal 하한, 근육 증가는 8% 증가·최대 300kcal 잉여를 적용한다.
- 매크로 비율은 균형/유지/근육 증가 `탄55·단20·지25%`, 감량 `탄50·단20·지30%`다. 식이섬유는 KDRI 연령·계산상 성별 충분섭취량을 사용한다.
- 미성년자, 임신·수유, 섭식장애 위험, 임상 영양 필요, 비정상 신체 입력, BMI 18.5 미만 감량, 75세 이상 체중 변경, 매우 높은 활동 수준은 자동 계산 대신 제한 모드로 전환한다.
- 생성된 `NutritionProfile`에는 입력 snapshot, EER, 활동계수, 목표 조정, 매크로 비율과 KDRI·정오표·engine·safety 버전을 저장한다. 변경 시 이전 행을 수정하지 않고 새 유효 버전을 생성한다.
- 앱의 오늘 화면과 `계산 기준` 탭에서 적용 기준·발행기관·정오표·엔진 버전·안전 제한과 공식 출처를 항상 확인할 수 있어야 한다.

### 9.7 온보딩 저장 계약

- 인증된 클라이언트는 `GET /api/onboarding/status`로 진입 상태를 확인하고, `POST /api/onboarding/preview`로 저장 없이 계산/제한 결과를 확인한 뒤 `PUT /api/onboarding/complete`로 확정한다.
- 모바일 흐름은 필수·선택 동의 → 목표 → 출생연도/계산상 성별 → 신장/체중 → 활동 수준/안전 분기 → 계산 결과 확인의 6단계다.
- 완료 요청은 서버가 현재 동의 문서 버전과 SHA-256을 사용해 모든 동의 결정을 append-only로 기록한다. 클라이언트가 문서 버전·해시나 계산 결과를 제출해 확정할 수 없다.
- `user_profile`, 동의 이벤트, 활성 목표 종료와 새 `NutritionProfile` 생성은 하나의 트랜잭션으로 처리한다. 제한 모드는 사유 코드만 저장하고 숫자 목표를 생성하지 않는다.
- `completed`와 `limited`는 terminal 상태다. 중복 완료 요청은 `409 ONBOARDING_ALREADY_COMPLETED`로 거부한다.

## 10. 안전 경계

| 영역 | 제품 행동 |
| --- | --- |
| 일반 웰니스 | 균형 잡힌 식사, 일반적인 목표 진행률, 식품 기반 추천 제공 가능 |
| 보충제 | 결핍 진단·용량·브랜드 처방 금지. “전문가와 상의” 안내 및 음식 기반 대안 우선 |
| 의료 | 질환 치료, 약물/검사 해석, 임상 식단 처방 금지. 의료진 상담 유도 |
| 고위험 신호 | 극단적 열량 목표, 섭식장애 표현, 임신/수유·미성년·특정 질환은 제한 모드/안내 |
| 알레르기 | 추천 생성의 hard constraint. 교차오염·제조시설 안전은 보장하지 않음을 명시 |
| 불확실성 | 정확한 측정이 아님을 명시하고, 낮은 신뢰도/범위/출처를 노출 |

모든 화면의 면책문구가 안전 기능을 대신하지 않는다. 위험 사용자 분기, 금칙 규칙, 추천 필터, 신고/감사 로그를 제품 로직으로 구현한다.

## 11. 비기능 요구사항

| 분류 | 요구 수준 |
| --- | --- |
| 성능 | 사진 제출 후 인식 결과 p95 8초 이내 목표; 대시보드 재계산 500ms 이내; 진행 상태 제공. |
| 가용성 | 핵심 기록/조회 월 99.5% 목표. 업로드/추론 장애 시 초안 보존과 재시도. |
| 보안/개인정보 | 전송·저장 암호화, 최소권한, 비밀 분리, 민감 로그 마스킹, 관리자 접근 감사. |
| 데이터 통제 | 이미지 학습 활용은 별도 opt-in. 내보내기/삭제. 보존기간과 백업 삭제 정책 명시. |
| 접근성 | 동적 글자, 스크린리더 라벨, 색 외 상태 표현, 44pt 터치 타깃, 한국어 우선. |
| 관측성 | 추론 지연/오류, DB 매핑률, 추천 필터링, 계산 버전, 클라이언트 크래시 모니터링. |
| 품질 | 영양 계산 단위 테스트 100%; 주요 한국 음식 골든셋; 모델/DB 회귀 리포트. |
| 플랫폼 | iOS 우선 또는 크로스플랫폼 단일 코드베이스. 네트워크 불안정 시 초안 로컬 보존. |

## 12. 분석 설계와 성공 지표

### 12.1 North-star metric

> **주간 유효 코칭 루프(WVCL):** 한 사용자가 같은 주에 “확정된 식사 기록 → 영양 격차 확인 → 다음 식사 추천 조회 또는 추천 기반 기록”을 완료한 횟수. 단순 로그 수보다 제품의 핵심 가치 전달을 측정한다.

### 12.2 KPI

| 퍼널/품질 | 지표 |
| --- | --- |
| 활성화 | 가입→온보딩 완료율, 첫 식사 확정률, 첫 가치 도달 시간(TTFV) |
| 습관 | D1/D7/D30 유지율, 주간 기록일 수, 사용자당 확정 식사 수 |
| 코칭 | WVCL 사용자 비율, 추천 조회율, 추천→기록 전환율, 교체율 |
| 정확도 | 음식 top-1 확인율, 사용자 교정률, DB 자동 매핑률, 양 수정률 |
| 신뢰/안전 | 출처 조회율, 오류 신고율, 알레르기 차단 위반 0건, 위험 응답 차단율 |
| 사업 | 무료→체험→유료 전환, 구독 유지, 환불률, 유료 의향 조사 |

### 12.3 핵심 이벤트

| 이벤트 | 주요 속성 |
| --- | --- |
| onboarding_started/completed | goal_type, steps_completed, duration, skip_count |
| meal_photo_submitted | meal_type, image_source, upload_latency |
| recognition_result_viewed | item_count, confidence_bands, model_version |
| meal_item_corrected | field(name/amount/unit), before/after category, confidence |
| meal_log_confirmed | item_count, duration, correction_count, source_quality_mix |
| dashboard_viewed | gap_count, day_progress, entry_point |
| recommendation_requested | context_mode, gap_types, constraints_count |
| recommendation_viewed | candidate_count, latency, engine_version |
| recommendation_actioned | action(log/swap/dismiss), candidate_rank |
| safety_intervention_shown | rule_id, surface, action; 원문 민감정보는 수집하지 않음 |
| subscription_viewed/started | trigger, plan, trial |

## 13. 수익화 가설

- Freemium: 기본 사진 기록·일일 대시보드는 무료, 고급 코칭/추세/상세 영양·무제한 추천은 구독.
- 무료 제한 후보: 하루 사진 기록 수 또는 주간 추천 횟수. 핵심 가치 체험을 막지 않는 선에서 실험.
- 개인 구독 가설: 월/연 요금제 + 7일 체험. 연간 플랜의 유지율과 초기 현금흐름 비교.
- 추후 B2B2C: 트레이너·웰니스 프로그램용 리포트. 의료 서비스로 오인되지 않게 별도 검토.

가격은 경쟁사 복제가 아니라 WTP 인터뷰, 페이월 클릭, 체험→유료, 4주 유지 코호트로 검증한다. MVP 초기에는 제품 학습을 위해 과도한 기능 잠금을 피한다.

## 14. 위험과 완화

| 위험 | 영향 | 완화 |
| --- | --- | --- |
| 사진/양 추정 오류 | 영양 합계 불신 | 확인 필수, 빠른 양 교정, confidence, 골든셋 |
| 한국 혼합식 다양성 | DB 매핑/정확도 저하 | 우선 카테고리, 별칭 ontology, 레시피 추정 표시 |
| 건강 위해/규제 | 사용자 피해·출시 차단 | 웰니스 경계, 위험 분기, 법률/임상 검토, 감사로그 |
| 개인정보 유출 | 중대한 신뢰 손실 | 최소수집, 암호화, 삭제, 이미지 학습 opt-in 분리 |
| 추천 단조로움 | 낮은 재방문 | 선호·최근 식사·조리 여건 반영, 교체 피드백 |
| 추론 비용/지연 | 단위경제성 악화 | 이미지 압축, 캐시, 모델 라우팅, 비용/성공 로그 |
| DB 라이선스/갱신 | 서비스 중단·수치 변동 | source registry, 버전 고정, 대체 출처, 변경 검증 |
| 창업자 범위 과다 | 출시 지연 | P0 루프 우선, 운영도구 최소화, 단계별 gate |

## 15. 단계별 로드맵

| 단계 | 목표 | 출구 조건 |
| --- | --- | --- |
| 0. Foundation · 1–2주 | 데이터 계약/스키마, 100개 핵심 음식, 계산 엔진, 클릭 프로토타입 | 20개 대표 식사 수동 테스트; 계산 재현 가능 |
| 1. Alpha · 3–5주 | 온보딩, 사진→확인→저장, 대시보드, 기본 추천 | 내부 30식 기록; P0 크래시 없음; 사용자 수정 가능 |
| 2. Closed beta · 3–4주 | 한국 음식 확대, 피드백, 안전 규칙, 분석/페이월 실험 | 20–50명 2주; 활성화/교정률 기준 충족; 안전 이슈 0 |
| 3. Public MVP | 스토어 출시, 구독, 고객지원/운영 모니터링 | WVCL·D7 코호트 추적; 치명 오류 대응 체계 |
| 4. Post-MVP | 미량영양소 베타, 개인화 추세, 외식/편의점 강화 | DB 품질·유지율·유료 신호에 따라 순차 gate |

## 16. 출시 체크리스트

- [ ] 제품: P0 여정 E2E, 수정/삭제/재시도, 빈 상태·오류 상태, 타임존/날짜 경계 검증
- [ ] 데이터: MFDS 등 출처 이용조건, 버전, 단위, 결측, 100g/1회 제공량 변환 검증
- [ ] AI: 한국 음식 골든셋, 낮은 신뢰도 분기, 모델/프롬프트 롤백, 비용 상한
- [ ] 안전: 알레르기 hard constraint, 위험 사용자 플로우, 의료/보충제 금칙 테스트, 전문가 검토
- [ ] 개인정보: 동의문, 처리방침, 이미지 학습 opt-in 분리, 계정/데이터 삭제, 침해 대응
- [ ] 품질: 크래시/성능/접근성/네트워크 테스트, 계산 단위·회귀 테스트, 스토어 기기 QA
- [ ] 운영: 오류 신고 큐, DB 매핑 수정, 고객지원 템플릿, 장애·모델 중단 대응
- [ ] 분석/사업: 이벤트 QA, KPI 대시보드, 페이월/구독 복원, 환불/문의 경로
- [ ] 출시: 앱 이름/상표 1차 확인, App Store 메타데이터·스크린샷·면책·심사 노트

## 17. 오픈 질문과 결정 로그

| 질문 | 권장 초기 결정 / 검증 |
| --- | --- |
| 플랫폼 | **결정:** Expo/React Native 단일 코드베이스, iOS 우선 출시. |
| 목표 계산식 | **결정:** 2025 KDRI EER·다량영양소·식이섬유 기준과 2026-03-16 정오표, 보수적인 감량/증량 한도 및 위험 사용자 제한 모드를 적용한다. |
| 정확도 임계값? | 음식/DB/양 각각 임계값을 두고 50–100식 베타로 보정. |
| 사진 저장/접근 | **결정:** 비공개 Railway Bucket과 단기 presigned access를 사용한다. 추론 원본은 최대 24시간, 512px 썸네일은 식사 삭제 시까지 보관하며 삭제 job으로 24시간 내 object 제거를 보장한다. |
| 무료 한도? | 첫 주 핵심 루프를 충분히 체험하도록 제한 없는 베타 후 사용 분포로 결정. |
| 추천 후보 생성? | 초기에는 큐레이션된 한국 식사 템플릿+수치 최적화. 완전 생성은 보류. |
| 미량영양소 노출? | MFDS 등 DB 커버리지/결측률 기준을 정한 뒤 항목별 베타. |
| 브랜드 리스크? | NUEAT으로 MVP 진행하되 스토어 제출 전 국내 상표·유사 앱 이름 검토. |
| 백엔드 인프라 | **결정:** Railway Bun/Fastify API + Better Auth + Drizzle + Neon PostgreSQL. |
| AI 모델 | **보류:** 한국 음식 골든셋 평가 후 제공자와 모델을 확정한다. |
| 런타임/패키지 관리 | **결정:** Bun 1.3.14를 고정하고 `bun.lock`을 단일 lockfile로 사용한다. Expo 보조 도구용 Node.js LTS는 유지한다. |
| 인증 방식 | **결정:** Better Auth 이메일 OTP만 사용한다. 소셜 로그인과 비밀번호 로그인은 제외한다. |
| 서비스 도메인 | **결정:** API는 `api-nueat.boseong.dev`, OTP 발신자는 기존 Resend 인증 도메인의 `NUEAT <auth@boseong.dev>`를 사용한다. |

## 18. 구현 시작점: 첫 2주 백로그

| 순서 | 산출물 |
| --- | --- |
| 1 | NutritionProfile, Food, NutrientProfile, MealLog/Item 스키마와 API 계약 |
| 2 | 영양 계산/단위 변환 순수 함수 + fixture 기반 단위 테스트 |
| 3 | 온보딩 6단계와 로컬/서버 상태 모델 |
| 4 | 카메라→업로드→mock 인식→교정 화면 vertical slice |
| 5 | 확정 기록 기반 일일 집계와 대시보드 |
| 6 | 큐레이션 추천 템플릿 20개 + 제약/격차 기반 랭킹 |
| 7 | 핵심 이벤트, 오류 로깅, feature flag |
| 8 | 대표 한국 식사 20개 E2E 테스트 및 5명 사용성 테스트 |

> **MVP 완료의 정의:** 사용자가 첫 식사를 신뢰할 수 있게 기록하고, 오늘의 격차를 이해하며, 다음 식사 후보를 행동으로 옮길 수 있다. 숫자가 많거나 AI가 화려한 것이 완료 기준은 아니다.

---

이 문서는 초기 제품·구현 가설이다. 사용자 조사, 데이터 품질, 안전 검토 결과에 따라 버전 관리한다.
